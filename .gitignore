# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Quarantined B2B healthcare VSL: 74MB, referenced by nothing, and it is the
# WRONG vertical's video (its poster reads "Your healthcare organization").
# It is kept locally only so the swap-in is easy; it must not enter git history,
# where it could never be removed without a rewrite. See the README.
/public/video/placeholder/
